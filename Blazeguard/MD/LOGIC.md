Lets Start with the logic of the project
Team member Work
Mahi-ML
ABHInandan-ML and GEN AI
Rishank-Frontend
Anshul(me)- BACKEND AGENTS INTERGRATON OF API GATEWAY DEPLOYMENT KAFKA 
ok
logic is that my friend is work on the two ml model 
1)Detection fire-which detect the the fire
2)Prediction -which predict the area of fire
ok i have to work next
first my friend use the yolo moldel which capture the picture which we take NASA FIRMS API then the picture is pass to yolo model then the ai model work it check that is prediction or the deteciton then it give the json file to two agent (detection model or the predicaiton if the case is deteciton then the detction model if the case is the prediciton then the predction ) json is passed KAFKA
stream line pipline then the json passed then our agent work to start then with these the logistcis ai agent is connected which aim is to find the shortest route the firestation and deploy resoruces handle real time logistics then this agent help in the also find the safest area for citize portal which we make separate with real time map we also create the citizealert ai agent to give alreat from the firestation and the resuce team for fire incident
we also make the self_involving which work with the prediction which increase the predicaiton we use long term memory pgvector for that 
protocal we used is A2A
5 ai agent
1) DETECTION AI AGENT
2) PREDICTION AI AGENT
3) LOGISTICS AI AGENT
4) CITIZEN ALERT AI AGENT
5) SELF ENVOLVING AGENT

now come the gen ai part we used the langchain short term mermory
 
